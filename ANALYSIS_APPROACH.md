# 저장소·Harness 병목 분석 접근 방식

## 문서의 역할과 계약 상태

목표·우선순위·마일스톤 완료 상태는 [ROADMAP.md](ROADMAP.md)가 관리한다. 이 문서는 문제 해결 방식, 공통 그래프·검색 제약, 미해결 방법의 검증 조건을 관리한다. ADR이 아니며 새 인터페이스나 구현 완료를 선언하지 않는다.

| 상태 | 의미 |
|---|---|
| 공통 제약 | 새 방향에서도 유지할 관측·근거·재현성의 경계 |
| 기존 기준선 | 이전 M1 계약에서 복원한 구체 규칙. 특정 profile의 분석 기준이며 모든 harness의 실제 행동은 아님 |
| 검토 후보 | M4·M6에서 실험 후 채택·수정·기각할 방법. 현재 기본값으로 확정하지 않음 |
| 부가 목표 전용 | 작업 seed/정답 target 시뮬레이션. 핵심 분석 계약에 포함하지 않음 |

기존 기준선은 `efec6d7^`의 ROADMAP에서 복원했다. 구현과의 전체 일치 여부를 이번 문서 정리에서 재감사한 것은 아니다. 실제 값·규칙 버전은 [agent-view profile](profiles/agent_view.v3.yaml), 구현된 자료형은 [그래프 모델](agent_view/models.py)에서 확인한다. 값이 바뀌는 곳을 이 문서에 중복 복사하지 않는다.

[연구 검토](research/a7f273b0e454e4d8-target-risk-feasibility.md)는 방법의 근거와 전이 한계를 관리한다. 작업별 시뮬레이션의 비용 분포·oracle 종료·합성 작업 계약은 Git에서 제외된 `local-reports/`의 부가 보고서에 남는다.

## 분석 단위와 흐름

| 분석 | 입력과 시작점 | 산출물의 역할 |
|---|---|---|
| 전역 병목 | 저장소 snapshot + harness profile. 분석 가능한 모든 위치 또는 명시적 표본 | 후보 과다·잘림·증거 분산·연결 제약을 가진 위치와 근거 |
| Target-only 검토 | 같은 입력 + 수정 target. 작업 seed 없이 알려진 수정 위치에서 출발 | 영향 후보·이해 계약·검증 수단 및 분석 불확실성 |
| 행동 대조 | M2~M7의 정적 결과·고정 trace 계약; M8의 실제 도구 출력·확인 기록 | M8에서 노출 가능성과 관측된 노출·확인의 차이 판정 |
| 개선 검증 | M2~M7의 변경 전후 snapshot/profile 준비; M8의 독립 실행 | M8에서 필요한 확인을 유지했는지와 실제 탐색 부담의 차이 판정 |

## 분석 모델

### 저장소 관계와 탐색 관계

저장소 엔터티의 관계와 에이전트가 다음 정보를 발견하는 관계를 구분한다. 같은 엔터티를 공유할 수 있지만, 탐색 가능한 연결이 곧 변경 영향 관계는 아니다.

- 저장소 관계: 호출, import, 타입·상속, 데이터·설정의 사용, 프레임워크 연결, 테스트·문서의 명시적 참조. 어댑터가 실제 추출하는 범위를 기록한다.
- 탐색 관계: 경로·내용 검색, 목록 조회, 노출된 식별자의 재검색, 좁히기 검색, 경로 해석, harness가 제공하는 인덱스나 repo map.
- 문서 언급은 탐색 단서와 문서 동기화 검토 후보가 될 수 있지만 런타임 영향의 증거로 자동 승격하지 않는다.
- 분석기 내부의 완전한 그래프를 실제 에이전트가 아는 정보로 주입하지 않는다. 인덱스나 map 제공 여부와 준비 비용은 harness의 명시적인 조건이다.

M1의 snapshot, occurrence, hint, query 동등성, 검색 잘림 및 결정적 직렬화는 재사용할 수 있는 기반이다. 기존의 특정 읽기 단위·검색 방식·비용 기본값이 모든 harness의 행동 계약을 정의하지는 않는다.

### Harness profile

검색 표면, 결과 순서·상한, 출력 형식, 파일 읽기 범위, 자동 주입 문서, 사전 인덱스, 탐색 순서, 병렬 실행, 하위 에이전트, 문맥 축약을 각각 명시한다. 지원하지 않는 동작은 분석 불가 또는 관측 전용으로 표시한다.

외부 지식에 의한 의미 추론을 근거 없는 정적 edge로 만들지 않는다. 의미 검색을 쓰는 harness는 관측된 결과를 고정해 평가하거나 현재 분석 범위 밖으로 표시한다. 규칙 기반 검색 결과를 그 harness의 실제 검색 결과로 대체해 주장하지 않는다.

프로필 값은 출처·버전·근거 상태를 가진다. 기존 `bfs-exhaust`, `hint-prior`, 재방문 0, 전체 읽기 단위 즉시 발견은 검증 전 가정이다. 프로필이 다르면 병목이 달라질 수 있다. M2~M7의 profile 비교는 정적 what-if이며, 같은 저장소에서 profile을 바꾼 실제 harness 효과 검증은 M8에서만 수행한다.

### 전역 분석

작업 없이 모든 분석 가능한 위치를 후보로 다룬다. 계산 예산 때문에 표본을 쓰면 대상 선정 규칙, 포함 범위와 제외 이유를 기록한다. 병목으로 이미 선정한 대상만으로 검증 집합을 구성하지 않는다.

- 후보 과다와 결과 잘림, refinement 필요성, 특정 단서에만 의존하는 연결.
- 증거의 분산, 우회 경로 부족, 순환·연결 단절, 높은 fan-in/out, 넓은 관련 후보 범위.
- 읽기 출력에 비해 확인해야 할 증거가 흩어진 위치.
- 미해결 동적·프레임워크 경계와 분석 대상에서 제외된 항목.

검색 진단에 사용하는 query/probe의 생성 규칙과 범위를 기록한다. 이는 저장소에서 재생성한 검사 집합이며 사용자가 주는 작업 seed가 아니다. 진단 빈도를 실제 작업 발생 빈도나 에이전트 방문 확률로 해석하지 않는다.

PageRank·중심성·영향 후보 크기는 병목 후보를 찾는 잠정 신호다. PageRank를 후보 선택에 적용할 예측적 유효성은 M8의 고정 검증 분할에서 검증하며, 그 전에는 후보 선택·위험 순위·작업량 추정에 사용하지 않는다. 큰 값 자체가 탐색 비용 증가나 위험을 증명하지 않는다.

### 수정 대상만 주는 영향·검토 후보 분석 — 미해결 연구 과제

목표 출력은 “이 대상을 수정할 때 이 항목을 확인할 구조적 이유가 있으며, 이 harness에서는 그 근거를 놓칠 조건이 있다”이다.

영향 후보, 대상을 이해하기 위한 의존 계약, 검증 수단을 구분해 보고한다. 호출의 역방향 도달만으로 세 집합을 통합하지 않는다. 테스트를 실제 분석된 관련 후보와 검증 수단에 포함하며, 종류만으로 누락 분석에서 일괄 제외하지 않는다.

관계별 전파 방향, 변경 종류별 조건, 전파 깊이와 종료 규칙은 M4의 연구·측정 결과로 정한다. 이전의 역방향 기본 전파, 양방향 계약 쌍, 형제 구현 전파, 이해 범위 깊이 1은 확정 계약에서 해제한다. 알 수 없는 관계 종류에 임의의 전파 의미를 부여하지 않고 미지원 경계로 보고한다.

전체 도달 범위가 저장소 대부분으로 커지는지, 소수 공용 노드가 이를 지배하는지 먼저 측정한다. 출력 예산으로 잘린 항목과 분석 규칙 밖의 항목을 구분한다. 그래프에서 연결이 없거나 제외됐다는 이유로 안전하다고 판정하지 않는다.

변경 내용이 없으므로 실제 파손 가능성이나 피해 크기를 수치로 단정하지 않는다. 우선은 관련 근거·조건·분석 한계를 제공하고, 위험 우선순위가 유효한지는 독립된 변경 사례로 검증한다.

### 노출과 누락

다음 상태를 분리한다.

1. 저장소에 근거가 존재하고 분석기가 추출했다.
2. 지정된 도구·출력 조건으로 근거를 노출할 수 있다.
3. 실행에서 해당 경로 또는 해당 코드 범위가 실제로 노출됐다.
4. 계약 검토, 해당 테스트 실행 등 사전에 정의한 확인 행동의 근거가 있다.

파일을 열었다는 사실만으로 전체 심볼의 발견·검토를 완료 처리하지 않는다. 검색 결과 잘림, 읽기 창 밖의 코드, 문맥 축약으로 보존되지 않은 근거는 서로 다른 관측 상태다. 확인 행동이 로그에 없으면 우선 “확인 근거 미관측”이며 곧바로 이해 실패나 버그 원인으로 단정하지 않는다.

정적 경로 부재는 지정된 도구·규칙 범위에서만 성립한다. 실제 누락 가능성의 순위·확률은 M8에서 에이전트 traces와 독립된 검토 기준으로 검증한 뒤 제시한다. 그전에는 `unverified`로 남기며 단순 접근성 가중치의 합을 누락 확률로 부르지 않는다.

## Snapshot과 읽기 표현

### Snapshot·제외·증거 보존 — 공통 제약과 기존 기준선

한 번의 분석은 변경되지 않는 snapshot을 사용한다. 분석 도중 다시 읽은 파일이 다른 상태가 되어 노드·검색 결과·비용이 서로 어긋나지 않게 한다. 분석기가 만든 출력은 명시적으로 식별해 재분석에서 제외한다.

기존 기준선은 lockfile, binary, build output, generated code 등을 읽을 노드에서 제외한다. 제외 규칙은 버전 관리하며 저장소별로 재정의할 수 있다. 이 제외는 분석 커버리지의 경계이며 “에이전트가 절대 읽지 않는다”거나 “수정 위험이 없다”는 판단이 아니다.

Generated 판별은 경로 glob과 파일 앞쪽의 고정된 줄 수 안에서 찾는 marker regex를 사용한다. glob·regex·줄 수는 설정으로 기록하며 내용에 대한 의미적 추측으로 판별하지 않는다. 제외 이유·규칙 버전·이유별 개수를 결과에 남긴다.

검색 출력 상한과 원본 occurrence 보존은 별개다. 에이전트에 보이는 출력은 제한하되 전체 occurrence 근거는 결정적인 압축 블록으로 보존한다. 압축된 전체 근거를 에이전트의 노출 정보로 사용하지 않는다.

### Readable node와 read unit — 기존 기준선

파일·모듈·클래스·함수·메서드·타입·설정·테스트·문서·endpoint가 readable node 후보이며 실제 지원 범위는 어댑터에 따른다. 내용 검색 occurrence의 도착점은 해당 위치를 포함하는 심볼 노드다. 경로 검색은 파일에 도착한다.

Read unit은 읽기 비용을 계산하는 단위이며 심볼 관계의 경계가 아니다. 기존 분할 규칙은 다음과 같다.

- 파일이 토큰 임계값 N 이하이면 하나의 read unit이다.
- 초과하면 소스 순서대로 심볼을 붙이다 다음 심볼이 N을 넘게 만들 때 새 chunk를 시작한다.
- 단일 심볼이 N보다 크면 그 심볼만으로 초과 크기 chunk를 만든다.
- 심볼 밖의 최상위 텍스트는 위치에 따라 인접 chunk에 붙인다.
- 다른 chunk는 별도 읽기 또는 해당 파일로 범위를 좁힌 탐색이 필요하다.
- N, 근거가 된 도구 제한, 분할 규칙 버전을 기록한다.

이전 비용 모델은 같은 read unit을 다시 방문하면 읽기 호출·토큰을 추가하지 않고 그 안의 target을 모두 발견했다고 간주했다. 이 동작은 부가 시뮬레이션의 가정이다. 핵심 행동 분석에서는 실제 반환된 줄·심볼 범위, 반복 반환 비용, 문맥 보존 여부를 구분하며 읽기 단위를 열었다는 이유로 검토 완료를 선언하지 않는다.

## 검색 계약 상세

### Exact·derived·path 검색 — 기존 기준선

Exact query는 명시된 단서를 그대로 검색한다. 식별자·qualified name·문자열·파일/모듈 경로·설정 키·URL/route·오류 메시지·문서와 주석의 코드 형태 표현이 대상이다.

Derived query는 exact 검색이 실패했을 때만 사용하는 fallback이 아니다. 노출된 단서에 버전 관리되는 규칙을 적용해 생성하는 별도 query 종류다. 기존 허용 변환은 다음과 같다.

- 대소문자·숫자 경계의 토큰 분리.
- 분리한 토큰과 인접 토큰의 조합.
- 대소문자 정규화와 등록된 단복수 변형.
- 규칙표에 등록된 접두사·접미사 제거.

외부 동의어 사전이나 의미 연상을 이 규칙의 결과로 취급하지 않는다. 내용과 정규화된 저장소 상대 경로를 모두 검색 표면으로 다루며, 경로 토큰에도 derived 규칙을 적용한다.

경로만 검색하는 동작은 list query다. 디렉터리 목록도 무료 노드가 아니라 출력 경로와 호출 비용을 가진 동작이다. 루트 목록의 깊이·항목 상한은 profile 조건이며 모든 실행의 필수 첫 행동은 아니다.

### 읽기에서 생성하는 query 후보 — 기존 기준선, 행동 적합성 미검증

모든 식별자에서 모든 질의를 생성하면 분석기가 실제 탐색보다 훨씬 많은 후보를 만들 수 있다. 기존 기준선은 다음 proxy로 후보를 제한한다.

1. 읽기 단위에서 나가는 정적 edge의 대상 식별자: 호출·import·상속·타입 참조.
2. 해당 읽기 단위에서 두 번 이상 등장하는 식별자.
3. 해당 읽기 단위에서 참조하지만 선언하지 않은 식별자.

필터 뒤에 읽기 단위별 후보 상한과 결정적인 절단 순서를 적용한다. 필터로 제외된 식별자 수와 상한 적용 여부를 query 기록에 남겨 저장소의 단서 부족과 모델의 후보 삭제를 구분한다.

이 규칙은 읽기에서 생성한 후보에만 적용한다. 검색 결과 hint에서 생성한 후보는 별도 제한을 사용한다. 새 harness에서 이 proxy를 채택할지는 M8의 실제 검색 기록과 대조하며, 그전에는 task relevance를 정확하게 복원한 규칙으로 주장하지 않는다.

전역 probe 생성에 이를 재사용하면 시작 위치·후보 생성 규칙·포함 범위·상한을 기록한다. 아직 읽지 않은 단서를 실제 에이전트가 이미 알고 있었다고 해석하지 않는다.

### Occurrence와 hint — 공통 정보 제약

Occurrence는 검색 근거이며 독립적으로 읽는 노드가 아니다. Hint는 query에서 readable node로 연결되는 edge의 속성으로, 파일을 열기 전에 검색 결과가 제공하는 정보다.

기존 hint 표현은 경로·파일명·심볼명, occurrence의 구문 역할, 노출된 줄 범위의 식별자 집합을 담는다. 역할에는 선언·import·호출·문자열·주석·문서 언급·테스트 등이 포함된다.

Hint는 이미 출력 비용에 포함된 정보만 사용할 수 있다. 분석기만 아는 enclosing symbol이나 구문 역할이 실제 출력에서 확인되지 않는다면 이를 에이전트에게 보인 사실로 취급하지 않는다. 실제 harness 출력과의 일치는 M8에서 검증한다.

기존 profile의 match-line/zero-context 출력은 하나의 기준선이다. 문맥 줄을 늘리면 hint 정보와 출력 토큰이 함께 늘어난다. Hint 자체를 새로운 비용 축으로 추가해 같은 출력을 중복 계산하지 않는다.

### 출력 상한과 후보 희석 — 기존 기준선

기존 검색은 occurrence 또는 출력 줄 수 상한을 사용하고, 정규화된 상대 경로와 줄 번호의 사전식 순서로 노출 결과를 결정한다. 상한 값·단위·순서 버전·근거 도구 설정을 기록한다.

- 노출된 occurrence에 대해서만 출력 토큰과 hint edge를 계산한다.
- 잘린 occurrence의 도착 노드는 그 query로 노출된 것으로 처리하지 않는다.
- 전체 occurrence 수와 잘림 여부를 보존해 과다 후보로 인한 노출 장애를 설명한다.
- 잘림이 없었던 것처럼 전체 결과를 비용에 넣거나 단서로 사용하지 않는다.

잘림 규칙은 실제 harness에 따라 앞부분 노출, 전체 억제, 페이지화 등으로 달라질 수 있다. 기존 기준선의 부분 노출을 다른 도구에도 적용하지 않는다. 지원하지 않는 출력 동작은 미지원으로 표시한다.

출력 상한과 refinement 임계값 K는 별개다. 전자는 실제로 보여주는 양이고 후자는 좁히기 동작을 생성하는 기준이다. 두 값의 상대 크기를 강제하지 않고 함께 기록한다.

### 읽기 없이 재검색하기 — 기존 기준선

Query → query edge는 검색 결과 줄에 보이는 식별자를 파일 읽기 없이 재검색하는 경우를 표현한다. 후보 식별자는 노출된 줄 범위에서만 취하고, 결과 그룹별 후보 상한을 적용하며, query 동등성 규칙으로 중복을 제거한다.

이 경로를 금지하면 이미 비용을 지불하고 노출된 단서를 활용하지 못한다. 반대로 제한 없이 확장하면 분석기의 후보 수가 부풀 수 있으므로 생성 범위와 제한을 모두 근거에 포함한다.

### Refinement — 기존 기준선

결과 그룹이 K를 넘으면 좁히기 query 후보를 생성한다. 후보는 이미 노출된 결과에 있는 디렉터리 경계의 경로 접두사, 확장자, 규칙표의 토큰 조합으로 제한한다. 저장소 전체를 아는 분석기 내부 정보로 좁힐 위치를 고르지 않는다.

Refinement는 query → query edge이며 별도 검색 호출이다. 결과의 전체 집합은 부모 query의 전체 결과 집합의 부분집합이지만, 새로 노출된 항목은 부모의 잘린 출력에는 없을 수 있다. 이 구분이 refinement가 추가 호출을 지불할 이유다.

깊이 상한·query별 후보 상한·K·적용 규칙을 기록한다. “노출된 부모 결과만의 부분집합”으로 구현해 잘린 항목을 영원히 발견할 수 없게 만들지 않는다.

### Query 동등성과 중복 제거 — 기존 기준선

Query의 식별 기준은 query 종류, 정규화된 문자열, 검색 표면, scope, 규칙표 버전의 조합이다.

- 생성 위치가 달라도 이 조합이 같으면 동일 query이며, 생성 근거는 여러 incoming edge로 보존한다.
- 문자열이 같아도 scope가 다르면 별도 query다. 파일 범위 refinement와 전역 query를 합치지 않는다.
- 이전 시뮬레이션은 이미 실행한 query의 재진입을 무료로 억제하고 횟수를 기록했다. 실제 실행에서 반복된 호출·출력 비용은 관측 비용에서 제거하지 않는다.

중복 제거는 그래프 정규화와 가상 실행의 규칙이다. 실제 재검색 비용과 중복 억제 카운터를 혼동하지 않는다.

### 연결의 특정성과 프레임워크 규칙 — 기존 기준선

| 연결 종류 | 의미 | 기록할 근거 |
|---|---|---|
| Uniquely identifying | 해석이 성공해 하나의 도착 위치를 특정함. 모델상 query 없이 읽기에 도달 가능 | 실제 resolver 결과와 경로 |
| Candidate-narrowing | 후보 범위만 좁혀 query 결과 그룹이 필요함 | 후보 범위·규칙·분기 수 |

Import나 qualified name도 resolver가 성공했을 때만 단일 도착점이다. Wildcard·re-export·dynamic import·복수 모듈 후보를 이름 형태만 보고 단일 해석으로 처리하지 않는다. 미해결 경계와 후보 집합을 기록한다.

프레임워크 규칙은 문자열 일치가 없어도 연결을 만들 수 있지만 어댑터가 규칙과 특정성 종류를 선언해야 한다. 분석기가 아는 프레임워크 관계를 해당 harness가 이용할 수 있는지는 별도 조건이다.

이전 M1 계획은 candidate-narrowing 표현·비용 지원과 실제 규칙 등록을 구분했다. 메시지 enqueue/dequeue와 event publish/subscribe 규칙은 후속 후보였다. 새 규칙이 edge를 추가하면 그래프·관련 분석 baseline을 다시 생성해야 하며, 그 차이를 구조 개선 효과로 취급하지 않는다.

### 진입 문서와 사전 정보 — 기존 기준선과 harness 조건

진입 문서 목록은 버전 관리되는 설정이며 README·AGENTS.md·CLAUDE.md 등을 포함할 수 있다. 어떤 문서를 자동 주입하고 어떤 문서를 실제로 읽는지는 harness별로 기록한다.

자동 주입은 읽기 호출 없이 토큰을 제공하는 경우를 표현한다. 문서에 언급된 파일은 자동으로 읽힌 것으로 처리하지 않는다. 경로나 식별자가 해석되면 별도 탐색 연결로 도달한다. 문서에 파일명을 나열하는 것만으로 그 파일의 발견 비용이 사라져서는 안 된다.

Repo map·인덱스를 제공하는 경우 생성 비용, 노출 내용과 예산을 기록한다. 기존 시뮬레이션의 map 기본 활성화는 모든 harness의 기본 상태가 아니다.

## Target-only 방법의 검토 후보

다음은 이전 구체 접근을 보존한 연구 후보다. 적용 방향·깊이·기본값을 확정한 표가 아니다.

| 후보 방법 | 이전 접근 | 검증 또는 수정할 조건 |
|---|---|---|
| 호출·import·타입 등의 의존 관계 | target의 역방향 도달 집합 | 실제 edge 의미·변경 종류에 따른 과다 후보와 누락 |
| 데이터·설정·event·message·route 계약 쌍 | 양쪽으로 전파 | 어느 쪽의 어떤 변경이 상대에 영향을 주는지, 정적 해석 커버리지 |
| 공통 base/interface의 구현들 | base를 경유한 sibling 탐색 | 한 구현 변경이 다른 구현 검토를 요구하는 조건. 무조건 전파 금지 |
| 수정 대상의 이해 계약 | 정방향 comprehension closure, 이전 깊이 1 | 영향 후보와 분리하고 필요한 깊이를 독립 사례로 평가 |
| 접근성 순위 | source kind·relation kind·거리·정적 confidence의 가중합 | 실제 노출·확인과의 관계, 가중치 근거, 미보정 순위의 한계 |
| 범위 제한 | 전파 깊이 또는 접근성 cutoff | 분석 생략과 도구 출력 잘림을 구분하고 후보에서 제거된 이유를 보존 |

기존의 미등록 관계 역방향 기본 전파와 테스트 일괄 순위 제외는 현재 계약이 아니다. 알 수 없는 관계는 미지원으로 남기고, 관련 테스트는 검증 수단과 누락 분석에 포함한다. 문서 언급은 문서 동기화 후보인지 런타임 관계인지 구분한다.

### 범위가 커지는 문제의 측정

완화책 선택 전에 다음을 측정하는 이전 gate를 유지한다.

- 깊이 1·2·3·무제한에서 `|후보 범위| / |readable nodes|` 분포.
- 같은 저장소의 target별 후보 크기 차이.
- 소수 high-fan-in 공용 노드가 범위 확장에 기여하는 비중.
- 분석 미지원과 제외 때문에 계산되지 않은 위치.

후보 완화책은 깊이 제한과 절단 표시, 깊이별 별도 축 보고, 공용 노드 기여의 별도 분류다. 먼저 하나를 고정하면 저장소 구조보다 그 완화 규칙을 측정할 수 있으므로 결과를 본 뒤 비교·선택한다. 출력 예산 밖의 후보를 안전하거나 불필요한 항목으로 취급하지 않는다.

### 후보 레코드와 체크리스트

각 후보는 target, 후보 종류, 경로, 적용 관계·방향·규칙, 코드/설정/문서 근거, 분석 confidence의 근거와 한계, 포함·제외·절단 이유를 가져야 한다. 여러 target의 결과를 합칠 경우 출처 target을 보존하고 겹치는 항목의 근거를 잃지 않는다. 전역 분석에도 같은 추적성이 필요하다.

접근성 성분과 관측된 노출·확인은 영향 근거와 별도 필드다. 낮은 접근성은 영향 후보에서 제거할 이유가 아니며, 관측 없이 “실제로 확인하지 않았다”고 기록할 이유도 아니다.

수정 전 체크리스트는 이 레코드에서 생성하는 검토 후보 목록이다. 순위가 검증되지 않으면 근거 목록으로 제공하고, 보정되지 않은 방문 확률이나 반드시 확인해야 할 정답 목록이라고 부르지 않는다. 정확한 JSON 스키마·API·오류 방식은 구현 전에 계약으로 확정한다.

## 병목 판정과 결과 추적성

전역 후보 생성에는 PageRank·중심성·연결 성분·k-hop 비용, cycle·bridge·articulation point, 후보 희석, 약한 탐색 연결, 반복 노출, 미해결 경계를 사용할 수 있다. PageRank를 후보 선택에 실제 적용하는 것은 M8의 고정 검증 분할에서 재현 가능한 예측 가치를 확인한 뒤에만 허용한다. 적용한 방향성·그래프 종류·집계 범위를 기록한다.

후보는 적어도 하나의 구체 장애로 설명한다. 유효 경로가 집중되는 위치, 비관련 결과로 출력이 소진되는 query, 특정 exact query에 의존하는 관계, 코드·설정·테스트의 분산, 관련 근거의 반복 노출과 확인 미관측 등이 대상이다. 비용 감소를 검증하지 않은 지표 순위는 잠정 후보다.

각 결과는 영향을 받는 위치·profile, 근거 node/query/edge/occurrence, 장애 또는 비용 축, 가정·미지원 범위, what-if와 실제 검증의 상태를 연결한다. 시뮬레이션 percentile을 핵심 결과의 필수 항목으로 요구하지 않는다.

결과에는 실제 사용한 snapshot·profile과 규칙 버전들을 기록한다. 분할·정렬·출력 형식·query 동등성·derived 규칙·tokenizer·제외·정책·비용 가중치 중 실제 소비한 항목을 남기며, 쓰지 않은 버전을 사용한 것처럼 표기하지 않는다.

토큰 근사와 실제 추론 비용은 구분한다. 기존 근사는 비숫자 문자 수를 C로 나누어 올림하고 숫자 run은 왼쪽부터 세 자리씩 묶어 센다. 값과 버전은 profile에 있다. 실제 모델 tokenizer·반복 문맥·캐시·출력 토큰을 측정하지 않은 근사를 API 청구 비용으로 해석하지 않는다.

기계 판독 JSON과 그 JSON만으로 생성한 HTML을 결과 전달 방식으로 유지한다. HTML이 별도 분석을 수행해 JSON과 다른 결론을 만들지 않는다. 산출물별 스키마와 불확실성·실패 상태는 해당 구현 전에 확정한다.

## 개선 효과 검증

M2~M7은 실제 harness 또는 agent를 호출하지 않는다. 각 단계는 정적·결정적 검사와 함께 M8에 필요한 snapshot, profile, 평가 사례, 확인 집합, 결과 기준과 실행 조건을 준비하며, 실제 관측이 필요한 결과는 `unverified`로 유지한다.

단계별 인계 자료와 trace 해석은 [Harness 평가 계약](research/4f38a2c1d760be95-harness-evaluation-contract.md#m2m7-handoff-to-m8)에서 관리한다. 고정 trace 재생의 통과는 선언한 계약의 일치만 뜻하며 실제 harness 행동의 검증 완료를 뜻하지 않는다.

M8은 고정된 입력으로 실제 harness를 반복 실행하고 구조 변경과 harness 변경을 별도 요인으로 비교한다. 실패·중단 실행도 포함해 검색·읽기 노출, 반복 호출, 준비 비용, 확인 근거, 작업 결과와 예측·관측 차이를 함께 판정한다. 필요한 확인과 작업 결과를 유지한 탐색 부담 감소만 실제 개선으로 인정한다.

## 구현 및 검증 연결

| 대상 | 기존 근거 또는 구현 | 변경 전 확인할 조건 |
|---|---|---|
| 읽기·query·occurrence·hint | `agent_view/`, `profiles/agent_view.v3.yaml` | 기능 결과·오류·불변성과 직렬화·출력·근거 재현성 |
| 언어·프레임워크 관계 | `language_analyzers/`, `framework_analyzers/` | 지원 관계와 resolver의 실제 의미, 미지원 경계 |
| 전역 지표 | `analysis/graph_metrics.py` | 지표의 그래프 범위·비용 가정과 장애 근거 |
| JSON 기반 보고서 | `report/m1/`, `report/shared/` | 결과와 표시의 일치, 근거 drill-down |

서명·반환 타입·오류 방식과 경계 사례의 기대 결과는 기능 구현 전에 정한다. 이 문서는 기존 공통 제약과 검토 후보를 제공하며, 새 인터페이스의 미정 세부사항을 대신 확정하지 않는다.
